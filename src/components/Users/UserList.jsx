import React from 'react';

import HeaderTitle from './../shared/HeaderTitle';

function UserList() {
  return <HeaderTitle title="User List" tagline="All the users we can find" />;
}

export default UserList;
